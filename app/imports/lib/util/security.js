export default class Security
{
    static makeCode(rules, user)
    {
        console.dir('checking the access');
        
        // console.dir(rules);
        // console.dir(user);

        return 200;
    }
}
